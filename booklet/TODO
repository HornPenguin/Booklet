

Clean `gui.py` module name add prefix 
Move `get_file_info` module from `utils.py` to `gui.py`

Implement progress bar routine to `Modifier` class


Imposition class에 문제 있음.
1. 크기 생성 및 배치 자체는 문제가 없으나
2. 이미지 page의 경우 보이지가 않음 + 일반 page는 특정 경우만 보임.